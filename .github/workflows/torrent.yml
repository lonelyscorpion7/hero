name: Create torrent
on:
  push:
    branches:
      - master
      
env:
  name: "Death In Venice (1971)"
  comment: "Death In Venice (1971)"
  url: https://nl25.seedr.cc/get_zip_ngen/106544064/Death%20In%20Venice%20(1971)%20[BluRay]%20[1080p]%20[YTS.AM].zip?st=u9dv3HjvvhN7h9RXTRQ7Jg&e=1608221425
  file_name: "Death In Venice (1971) [BluRay] [1080p] [YTS.AM].zip"
  piece_size: 20 # Check the README.md for recommended piece size
      
jobs:
  create:
    runs-on: ubuntu-20.04
    steps:
      - name: Install mktorrent
        run: sudo apt-get install mktorrent
        
      - name: Download file
        run: wget -nv -O "$file_name" "$url"
        
      - name: Create torrent using mktorrent
        run: mktorrent -c "$comment" -l $piece_size -o "${name}.torrent" -w "$url" "$file_name"
        
      - name: Upload torrent file
        uses: actions/upload-artifact@v2
        with:
          name: ${{ env.name }}
          path: ${{ env.name }}.torrent
