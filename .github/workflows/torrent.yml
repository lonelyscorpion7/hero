name: Create torrent
on:
  push:
    branches:
      - master
      
env:
  name: "The.Wizard.of.Gore.1970.1080p.BluRay.x264.YIFY"
  comment: "The.Wizard.of.Gore.1970.1080p.BluRay.x264.YIFY"
  url: https://dlsvfree.sonicbit.net/file/dl2/aWgvMUZ4WnlKVnlKcURnYkY2RUFIQ2R6bEhOaU1BWU5WRDM3a2dkWGoxay9TL0c0bUhPQThvR3h6MjQrd0F5UVBGcFk5RDRNYURWcVJLU1NaWEN5NjgyZys1eEVrS0poUWY1WnBPYXlpOEZOZ002QmlKU2w4K0YvSlduTDlWU0s3a0xBaWlLY3NyQ244MTUzd2RjSHYyNkdpUGVndzNiUEI1ZWh4MDJDY0NzPQ==/The.Wizard.of.Gore.1970.1080p.BluRay.x264.YIFY.mp4
  file_name: "The.Wizard.of.Gore.1970.1080p.BluRay.x264.YIFY.mp4"
  piece_size: 20 # Check the README.md for recommended piece size
      
jobs:
  create:
    runs-on: ubuntu-20.04
    steps:
      - name: Install mktorrent
        run: sudo apt-get install mktorrent
        
      - name: Download file
        run: wget -nv -O "$file_name" "$url"
        
      - name: Create torrent using mktorrent
        run: mktorrent -c "$comment" -l $piece_size -o "${name}.torrent" -w "$url" "$file_name"
        
      - name: Upload torrent file
        uses: actions/upload-artifact@v2
        with:
          name: ${{ env.name }}
          path: ${{ env.name }}.torrent
